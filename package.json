{
  "name": "vanilla-x",
  "version": "0.0.1",
  "description": "Extensão dos protótipos da API Javascript nativa",
  "main": "index.js",
  "repository": "git@github.com:FilipeBeck/vanilla-x.git",
  "author": "Filipe Roberto Beck",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "reset && jest --coverage",
    "coveralls": "cat ./coverage/lcov.info | coveralls",
    "build": "reset && tsc && cp package.json README.md ./app && rm app/tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "@types/node": "^12.0.10",
    "io-ts": "^1.10.2",
    "jest": "^24.8.0",
    "ts-infer": "0.1",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "ts-toolbelt": "^3.8.63"
  }
}
